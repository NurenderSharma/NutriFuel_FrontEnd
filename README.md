# NutriFuel Frontend

Vite + React frontend for the NutriFuel application.

```bash
npm install
npm run dev
```

The development app runs at `http://localhost:5173`.

Useful commands:

```bash
npm run build
npm test
```
